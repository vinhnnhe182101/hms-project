import { Container, Title, Text, Box } from '@mantine/core';

export default function BookingHistoryPage() {
    return (
        <Box py={80}>
            <Container size="xl">
                <Title order={1} mb="md">Booking History</Title>
                <Text c="dimmed">Booking history page is under development...</Text>
            </Container>
        </Box>
    );
}
